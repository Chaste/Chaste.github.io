name: Check links on PRs

on:

  pull_request:
    branches: ["**"]

  push:
    branches: ["main"]
    
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linkcheck:

    timeout-minutes: 20
    runs-on: ubuntu-24.04
    env:
      HUGO_VERSION: 0.144.2

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false

      - name: Setup python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.13'

      - name: Install LinkChecker
        run: |
          pip install --upgrade pip setuptools wheel
          pip install linkchecker

      - name: Install Hugo
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
          hugo version

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '20'
          
      - name: Build site
        run: |
          npm install
          hugo --baseURL="http://localhost:8002"
        working-directory: site
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
          TZ: Europe/London

      - name: Serve site and run LinkChecker
        run: |
          python -m http.server 8002 --directory site/public/ > /dev/null 2>&1 &
          sleep 2
          linkchecker --config=".linkcheckerrc" --timeout=120 --threads=4 http://localhost:8002/
          
      - name: Generate summary
        if: always()
        run: |
          bash generate-summary.sh
          cat broken-links.md > $GITHUB_STEP_SUMMARY
